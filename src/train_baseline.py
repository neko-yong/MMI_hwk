"""Entry point for the future SVM baseline experiment."""

from __future__ import annotations

from src.utils import set_random_seed


def main() -> None:
    """Run the minimal baseline placeholder."""
    set_random_seed(42)
    print("Baseline scaffold is ready. DEAP loading and SVM training will be added next.")


if __name__ == "__main__":
    main()
